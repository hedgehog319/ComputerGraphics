<Window x:Class="ComputerGraphics.Windows.Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:main="clr-namespace:ComputerGraphics.Windows.Main"
        mc:Ignorable="d"
        Title="DSP" Height="450" Width="800" Background="DarkGray">
    <Window.DataContext>
        <main:MainWindowModel />
    </Window.DataContext>

    <Grid>
        <Menu Height="18" VerticalAlignment="Top">
            <MenuItem Header="Файл">
                <MenuItem Header="Открыть" Click="Open_Click" />

                <MenuItem Header="Сохранить" />
            </MenuItem>

            <MenuItem Header="Вид" />

            <MenuItem Header="Инструменты">
                <MenuItem Header="Информация" Click="Information_OnClick" IsEnabled="{Binding Path=IsFileOpen}" />
            </MenuItem>

            <MenuItem Header="Окна">
                <MenuItem Header="Окно навигации" Click="OpenNavMenu_Click" IsEnabled="{Binding Path=IsNavBarMayOpen}" />
            </MenuItem>

            <MenuItem Header="О программе">
                <MenuItem Header="Разработчики" Click="AboutProgramm_Click" />
            </MenuItem>
        </Menu>
    </Grid>
</Window>